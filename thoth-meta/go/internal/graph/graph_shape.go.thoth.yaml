locator: internal/graph/graph_shape.go
meta:
  purpose: "Implements graph operations in graph_shape.go so report generation and analysis can operate on deterministic graph views."
  responsibilities: "select graph subsets; evaluate shape/properties; render or traverse graph structures; support query helpers used by CLI/report flows"
  architecture_notes: "Graph code keeps selection, shape detection, and rendering separated to prevent coupling traversal rules with presentation concerns."
  function_list:
    - DetectShape
    - buildAdj
    - hasCycle
    - isTree
    - noteIDs
    - pruneFromStart
    - roots
    - visitCycle
    - walk
  import_files_list:
    - github.com/flarebyte/baldrick-flying-buttress/internal/domain
    - github.com/flarebyte/baldrick-flying-buttress/internal/ordering
  language: go
