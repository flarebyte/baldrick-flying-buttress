locator: internal/validate/app_data_registry.go
meta:
  purpose: "Provides validation logic in app_data_registry.go to keep app configuration and graph semantics safe and deterministic."
  responsibilities: "validate structured inputs; enforce policy and schema constraints; enrich diagnostics with stable context; normalize data for downstream steps"
  architecture_notes: "Validation is decomposed by concern (schema, args, labels, graph integrity, diagnostics) to keep rule changes localized and testable."
  function_list:
    - containsScope
    - containsString
    - isLabelReferenceArgument
    - isValidScope
    - isValidValueType
    - normalizeAllowedValues
    - normalizeAllowedValuesWithDuplicateInfo
    - normalizeDefaultValue
    - normalizeScopes
    - parseConfiguredArgument
    - resolveRegistry
    - validateRegistry
    - valueMatchesType
  import_files_list:
    - github.com/flarebyte/baldrick-flying-buttress/internal/domain
    - github.com/flarebyte/baldrick-flying-buttress/internal/textutil
    - slices
    - strconv
    - strings
  language: go
