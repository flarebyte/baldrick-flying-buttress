locator: internal/validate/app_data_schema.go
meta:
  purpose: "Provides validation logic in app_data_schema.go to keep app configuration and graph semantics safe and deterministic."
  responsibilities: "validate structured inputs; enforce policy and schema constraints; enrich diagnostics with stable context; normalize data for downstream steps"
  architecture_notes: "Validation is decomposed by concern (schema, args, labels, graph integrity, diagnostics) to keep rule changes localized and testable."
  function_list:
    - buildRawAppValue
    - compileSchemaAndApp
    - isReportSectionsPath
    - mapSchemaDiagnostic
    - toCanonicalPath
    - validateStructure
  import_files_list:
    - cuelang.org/go/cue
    - cuelang.org/go/cue/cuecontext
    - cuelang.org/go/cue/errors
    - embed
    - encoding/json
    - fmt
    - github.com/flarebyte/baldrick-flying-buttress/internal/domain
    - github.com/flarebyte/baldrick-flying-buttress/internal/load
    - strconv
    - strings
  language: go
