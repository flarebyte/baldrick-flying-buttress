locator: internal/validate/app_data_graph_integrity.go
meta:
  purpose: "Provides validation logic in app_data_graph_integrity.go to keep app configuration and graph semantics safe and deterministic."
  responsibilities: "validate structured inputs; enforce policy and schema constraints; enrich diagnostics with stable context; normalize data for downstream steps"
  architecture_notes: "Validation is decomposed by concern (schema, args, labels, graph integrity, diagnostics) to keep rule changes localized and testable."
  function_list:
    - checkCrossReportReferences
    - checkDuplicateNoteNames
    - checkMissingNodes
    - checkOrphans
    - collectNoteReportMembership
    - collectReportSectionNoteNames
    - hasReportOverlap
    - normalizeCrossReportPolicy
    - normalizePolicySeverity
    - resolveGraphIntegrityPolicy
    - validateGraphIntegrity
  import_files_list:
    - fmt
    - github.com/flarebyte/baldrick-flying-buttress/internal/domain
    - slices
    - strings
  language: go
